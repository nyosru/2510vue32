name: Deploy livewire

on:
  push:
    branches:
      - dev

env:
  VPS_IP: 91.218.230.97
  VPS_USERNAME: root
  VPS_USER: root
  DIR: /home/2510vue32
  GIT_BRANCH: origin/dev

jobs:


#  deploy1:
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v3
#
#      - name: Setup SSH key
#        run: |
#          mkdir -p ~/.ssh
#          chmod 700 ~/.ssh
#          echo "${{ secrets.DEPLOY_KEY }}" > ~/.ssh/id_rsa
#          chmod 600 ~/.ssh/id_rsa
#          #ssh-keyscan -H ${{ secrets.VPS_HOST }} >> ~/.ssh/known_hosts
#          ssh-keyscan -H ${{ env.VPS_IP }} >> ~/.ssh/known_hosts
#
#      - name: Deploy to VPS
#        run: |
#          ssh ${{ env.VPS_USER }}@${{ env.VPS_IP }} << EOF
#            cd ${{ env.DIR }}
#            git fetch --all
#            git reset --hard ${{ env.GIT_BRANCH }}
#            # git pull origin cicd_to_ihc_docker24
#            # Добавьте команды для сборки, перезапуска сервисов, например:
#            # docker-compose down
#            # docker-compose up -d --build
#          EOF

  deploy:
    runs-on: ubuntu-latest
    steps:

      - name: "refresh git & restart docker"
        uses: appleboy/ssh-action@master
        with:
          host: ${{ env.VPS_IP }}
          username: ${{ env.VPS_USERNAME }}
          key: ${{ secrets.DEPLOY_KEY }}
          #password: ${{ secrets.PASSWORD }}
          script: |
            cd ${{ env.DIR }}
            git fetch --all
            git reset --hard ${{ env.GIT_BRANCH }}


      - name: "update composer migration"
        uses: appleboy/ssh-action@master
        with:
          host: ${{ env.VPS_IP }}
          username: ${{ env.VPS_USERNAME }}
          key: ${{ secrets.DEPLOY_KEY }}
          #password: ${{ secrets.PASSWORD }}
          script: |
            docker exec 2510vue32 composer i --no-dev
            docker exec 2510vue32 npm run build
            docker exec 2510vue32 php artisan migrate
            docker exec 2510vue32 php artisan view:clear
            docker exec 2510vue32 php artisan cache:clear
            docker exec 2510vue32 php artisan config:cache


  # deploy_composer_stop:
  #   runs-on: ubuntu-latest
  #   needs: deploy
  #   steps:
  #     - uses: appleboy/ssh-action@master
  #       with:
  #         host: ${{ env.VPS_IP }}
  #         username: ${{ env.VPS_USERNAME }}
  #         key: ${{ secrets.DEPLOY_KEY }}
  #         script: |
  #           cd ${{ env.DIR }}
  #           docker-compose down

  # deploy_composer:
  #   runs-on: ubuntu-latest
  #   needs: deploy_composer_stop
  #   steps:
  #     - uses: appleboy/ssh-action@master
  #       with:

  #         host: ${{ env.VPS_IP }}
  #         username: ${{ env.VPS_USERNAME }}
  #         key: ${{ secrets.DEPLOY_KEY }}

  #         script: |
  #           cd ${{ env.DIR }}
  #           make prod

  sms_start:
    runs-on: ubuntu-latest
    steps:
      - name: send telega
        uses: appleboy/telegram-action@master
        with:
          # to: ${{ secrets.TELEGRAM_TO }}
          # phpcat,
          to: 360209578
          token: ${{ secrets.TELEGRAM_TOKEN }}
          message: |
            🐥🐥🐥 ${{ github.repository }}
            ${{ github.event_name }} > ${{ github.event.head_commit.message }}
            👨🏻‍💻 ${{github.actor}} (${{ github.run_id }})

  sms_end:
    runs-on: ubuntu-latest
    # needs: [deploy_composer, deploy_npm]
    needs: deploy
    steps:
      - name: send telega
        uses: appleboy/telegram-action@master
        with:
          # phpcat,
          to: 360209578
          token: ${{ secrets.TELEGRAM_TOKEN }}
          message: |
            🍀🍀🍀 ${{ github.repository }}
            ${{ github.event_name }} > ${{ github.event.head_commit.message }}
            👨🏻‍💻 ${{github.actor}} (${{ github.run_id }})
